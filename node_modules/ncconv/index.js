"use strict";

try {
  module.exports = require(`./bin/${process.platform}_${process.arch}/ncconv.node`);
}
catch (error) {
  console.error(error);
  throw new Error("Unable to load ncconv binary. Unsupported platform/architecture or Node.js version.");
}
